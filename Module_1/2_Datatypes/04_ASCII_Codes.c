#include <stdio.h>

int main() {
    char character='A'; //single quote
    char character2='B';
    char c3= 67;
    int Ascii= (int)character;
    printf("Ascii code for %c is %d\n", character, Ascii);
    printf("Ascii code for %c is %d\n", character2, character2);
    printf("Ascii code for %c is %d\n", c3, c3);
    return 0;
}
// In C, ASCII (American Standard Code for Information Interchange) codes are a standard way to represent characters using numeric values. ASCII assigns a unique numerical value to each character.

// For example:

// The ASCII code for the letter 'A' is 65.
// The ASCII code for the digit '0' is 48.
// The ASCII code for the space character (' ') is 32.
